apiVersion: v1
kind: Service
metadata:
  name: assistentes-gpt-backend-homolog-service
  namespace: central-oondemand-homolog
spec:
  type: NodePort
  selector:
    app: assistentes-gpt-backend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
